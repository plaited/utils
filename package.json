{
  "name": "@plaited/utils",
  "version": "1.0.3",
  "engines": {
    "node": ">= 14.2.0",
    "npm": ">= 6.14"
  },
  "main": "src/index.js",
  "module": "src/index.js",
  "files": [
    "src/"
  ],
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "scripts": {
    "lint": "eslint . --quiet --fix --ext .js",
    "lint:ci": "eslint .  --ext .js",
    "lint:fix": "npm run lint -- --fix",
    "test": "ava",
    "test:ci": "c8 --check-coverage --statements 90 --branches 90 --functions 90 --lines 90 --exclude client/**/*.spec.js npm test",
    "test:watch": "npm test -- --watch --verbose"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
      "pre-commit": "npm run lint && npm run test:ci && format-package -w"
    }
  },
  "ava": {
    "extensions": [
      "js"
    ],
    "files": [
      "**/*.spec.js"
    ],
    "require": [
      "esm"
    ]
  },
  "types": "src/index.d.ts",
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "ava": "^3.13.0",
    "c8": "^7.3.5",
    "cz-conventional-changelog": "^3.3.0",
    "eslint": "^7.12.1",
    "eslint-plugin-compat": "^3.8.0",
    "esm": "^3.2.25",
    "format-package": "^6.1.0",
    "husky": "^4.3.0",
    "sinon": "^9.2.1"
  }
}